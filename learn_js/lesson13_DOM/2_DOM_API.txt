// HTML DOM
// HMTL DOM ko phải của JS
// Là quy chuẩn mô tả mô hình của các thành phần trong tài liệu HTML được đưa ra bởi W3C.

// 1. Element
// 2. Attribute
// 3. Text

// Javascript:
// Javascript (JS) cung cấp bộ cung cụ giúp chúng ta truy cập được vào cấu trúc của HTML DOM
// Javascript chạy đc ở browser (trình duyệt) | server (node JS);


// Browser: HTML -> DOM -> DOM API (WEB API)
// API: Application programing interface
// DOM API: Là bộ API nằm trong web API có mặt trên những môi trường hỗ trợ duyệt web - như trên trình duyệt. 
// DOM API cung cấp các đối tượng và phương thức hỗ trợ truy xuất, chỉnh sửa các đối tượng/ thành phần trong DOM.
// DOM ko phải của JS
// DOM chỉ là 1 web api đc viết bằng js hoặc ngôn ngữ lập trình thấp hơn và đc tích hợp vào web
