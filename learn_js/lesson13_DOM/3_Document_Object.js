// document là đại diện cho toàn bộ website.
// muốn truy xuất bất kì đối tượng nào thì đều phải đi qua document.

document.write('Hello dom'); // ghi vào html 1 đoạn text, nội dung được khi ở sau element cuối cùng torng body.(Đặt ở đâu thì nằm ở đó trong html)

// sang tab console, chỉ cẩn chạy đoạn mã đó thì sẽ overwrite DOM.