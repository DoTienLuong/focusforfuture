// Trong JavaScript là ngôn ngữ động bộ nhưng vẫn luốn có những thao tác bất đồng bộ.

// Sync (đồng bộ): 
// Theo tư duy đồng bộ code nào viết trc thì chạy trc
// Async (bất đồng bộ)
// Những function hoạt động theo cách bất động bộ (viết trc nma xài sau)
// setTimeout, setInterval, fetch, XMLHttpRequest,
// FileReader, requestAnimationFrame


























